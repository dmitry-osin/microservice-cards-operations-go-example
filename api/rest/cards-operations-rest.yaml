openapi: 3.0.3
info:
  title: Title
  description: Title
  version: 1.0.0

components:
  schemas:
    CardOperations:
      type: object
      required: [ event-id, card-id, status, transaction_date_time ]
      properties:
        event-id: { type: string, format: uuid }
        card-id: { type: string, format: uuid }
        status: { type: string, enum: [ APPROVED, REJECTED, IN_PROGRESS ] }
        transaction_date_time: { type: string, format: date-time }

paths: { }
